<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
    <EditTextPreference
        android:key="postal_code"
        android:title="Postal Code"
        android:defaultValue="32826"
        android:selectAllOnFocus="true"
        android:inputType="number"
        android:singleLine="true" />

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:key="temperature_units"
        android:title="Temperature Units"
        android:defaultValue="metric"
        android:entries="@array/TemperatureUnitOptions"
        android:entryValues="@array/TemperatureUnitValues" />

</PreferenceScreen>
